Simple python script that downloads all images from a provided URL to the disk
Create a new Images_Webpage with providing a valid url and call find_images() to scrape for all image elements in the html code.
Call download_all_images and provide a folder in which you want the output to flow. 